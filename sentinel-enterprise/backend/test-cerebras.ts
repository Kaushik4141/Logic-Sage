import 'dotenv/config'; import { generateText } from 'ai'; import { createOpenAI } from '@ai-sdk/openai'; const cerebras = createOpenAI({ baseURL: 'https://api.cerebras.ai/v1', apiKey: process.env.CEREBRAS_API_KEY }); async function main() { try { const res = await generateText({ model: cerebras.chat('llama3.1-8b'), prompt: 'hello' }); console.log(res.text); } catch (e) { console.error('Error:', e); } } main();
